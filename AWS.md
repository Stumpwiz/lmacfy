# AWS App Runner Deployment Guide

This guide covers deploying the LMACFY application to AWS App Runner using Amazon ECR (Elastic Container Registry).

## Prerequisites

- ✅ AWS CLI installed and configured (`aws configure`)
- ✅ AWS account with appropriate permissions (ECR, App Runner)
- ✅ Docker Desktop running
- ✅ Application tested locally with Docker

## Architecture
